name: laconic
description: A laravel like sql query builder for mysql designed to be flexible, portable and easy to use.
version: 0.0.1
homepage: https://laconic.antdf.xyz

environment:
  sdk: ">=3.7.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15

dependencies:
  mysql1: ^0.20.0
  sqlite3: ^2.7.5
